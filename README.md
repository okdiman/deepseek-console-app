# 🚀 DeepSeek Console Application

Простое и эффективное консольное приложение для работы с DeepSeek AI API.

## 📁 Структура проекта

```
deepseek-console-app/
├── main.py              # Основное приложение
├── test_api.py          # Тест API соединения
├── requirements.txt     # Python зависимости
├── .env.example         # Пример файла с переменными окружения
├── .gitignore          # Git ignore файл
└── README.md           # Документация
```

## 🚀 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/okdiman/deepseek-console-app.git
cd deepseek-console-app
```

### 2. Создание виртуального окружения
```bash
python3 -m venv venv
```

### 3. Активация виртуального окружения

**На macOS/Linux:**
```bash
source venv/bin/activate
```

**На Windows:**
```bash
venv\Scriptsctivate
```

### 4. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 5. Настройка API ключа
Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

Откройте файл `.env` и добавьте ваш API ключ DeepSeek:
```
DEEPSEEK_API_KEY=your_deepseek_api_key_here
```

### 6. Запуск приложения
```bash
python3 main.py
```

## 🧪 Тестирование API соединения

Перед первым запуском рекомендуется протестировать API:
```bash
python3 test_api.py
```

Если все работает корректно, вы увидите:
```
Status: 200
Response: API test successful
```

## 🎮 Использование приложения

### Команды:
- **Любой текст** - отправить вопрос AI
- **/help** - показать справку
- **/quit** или **/exit** - выход из приложения

### Примеры вопросов:
- `Объясни машинное обучение простыми словами`
- `Напиши функцию сортировки массива на Python`
- `Переведи на английский: Привет, как дела?`
- `Что такое блокчейн?`

### Пример сессии:
```
============================================================
🚀 DeepSeek Console Application
============================================================
Commands:
- Type any question to get AI response
- /help - Show this help
- /quit or /exit - Exit application
============================================================

Your message: Привет, как дела?
🤖 AI: Processing...
🤖 AI: Привет! У меня всё отлично, спасибо за вопрос! 
Я готов помочь вам с любыми задачами...

Your message: /quit
👋 Goodbye!
```

## ⚙️ Технические детали

- **Язык**: Python 3.8+
- **Архитектура**: Простая, весь код в одном файле `main.py`
- **Асинхронность**: asyncio + aiohttp для эффективных HTTP запросов
- **API**: DeepSeek Chat Completions API
- **Конфигурация**: Переменные окружения через python-dotenv

## 🛠️ Зависимости

- `aiohttp` - для асинхронных HTTP запросов
- `python-dotenv` - для работы с переменными окружения

## 📝 Получение API ключа DeepSeek

1. Зарегистрируйтесь на [DeepSeek](https://platform.deepseek.com/)
2. Перейдите в раздел API Keys
3. Создайте новый API ключ
4. Скопируйте ключ в файл `.env`

## 🔒 Безопасность

- Файл `.env` с API ключом не попадает в git (добавлен в `.gitignore`)
- Используйте только собственный API ключ
- Не делитесь API ключом с другими

## 🐛 Устранение проблем

### Ошибка "DEEPSEEK_API_KEY not found"
- Убедитесь, что файл `.env` создан и содержит правильный API ключ
- Проверьте, что файл находится в корневой папке проекта

### Ошибка сети
- Проверьте подключение к интернету
- Убедитесь, что API ключ действующий

### Python версия
- Требуется Python 3.8 или выше
- Проверьте версию: `python3 --version`

## 📄 Лицензия

MIT License

## 🤝 Вклад в проект

Пулл реквесты приветствуются! Для крупных изменений сначала откройте issue.

---

**Готово к использованию!** 🎉

Просто следуйте инструкциям выше и наслаждайтесь общением с DeepSeek AI!
